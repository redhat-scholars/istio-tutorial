apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: worldclockapi-egress-rule
spec:
  hosts:
  - worldtimeapi.org 
  ports:
  - name: http-80
    number: 80
    protocol: http
